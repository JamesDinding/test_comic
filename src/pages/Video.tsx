import { h, FunctionalComponent } from "preact";

const VideoPage: FunctionalComponent = () => {
    return (
        <div class="grow">
            video
        </div>
    );
};

export default VideoPage
