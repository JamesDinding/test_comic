import { h, FunctionComponent, createContext } from "preact";
import { useState, useEffect, useContext } from "preact/hooks";

// storing home page data i.e. recommend, category, search & swiper
const MemoContext = createContext({});

// export const MemoProvider: FunctionalComponent = ({  }) => {
//   const [] = useState();
// };
